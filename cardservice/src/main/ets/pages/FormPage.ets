// cardservice form page — placeholder
// W1 块 3 之后实装: TodayReviewCard 等卡片 UI

@Entry
@Component
struct FormPage {
  build() {
    Column() {
      Text('MindTrace Card')
        .fontSize(16)
        .fontColor('#333333')
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
